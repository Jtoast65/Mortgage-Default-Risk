# Data ingestion / storage
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0
duckdb>=1.0.0             # parse pipe-delimited files to parquet; SQL over ~90M perf rows
openpyxl>=3.1.0          # read the official file-layout .xlsx when refreshing layouts.py

# ML
scikit-learn>=1.4.0        # isotonic calibration, logistic regression, metrics
xgboost>=2.0.0
joblib>=1.3.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0              # used by tests and keep-warm checks
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=8.0.0

# Plots (reliability curve, vintage panel, EDA)
matplotlib>=3.8.0
